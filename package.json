{
  "name": "dma-leads-webhook",
  "version": "1.0.0",
  "description": "Webhook receiver that keeps the DMA & BuyAndRentRobots Master Leads Tracker (Google Sheets) in sync with GHL and CheckCherry",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "googleapis": "^140.0.0"
  }
}
